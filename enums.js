module.exports.DURATION = Object.freeze({
    DAY      :   "day", 
    WEEK     :   "week",
    MONTH    :   "month", 
    YEAR     :   "year", 
});

module.exports.ENDS_TYPE = Object.freeze({
    NONE        :   "none", 
    DATE        :   "date", 
    OCCURENCE   :   "occurence", 
});

module.exports.EXPENSE_TYPE = Object.freeze({
    ONETIME        :   "oneTime", 
    RECCURING      :   "reccuring"
});

module.exports.PAYMENT_STATUS = Object.freeze({
    EXPIRE        :   "expire", 
    PAID          :   "paid"
});
